{
  "name": "opencode-web-blocker",
  "private": true,
  "workspaces": [
    "packages/shared",
    "packages/server",
    "packages/chrome-extension",
    "packages/opencode-plugin"
  ],
  "scripts": {
    "dev": "bun --filter '*' dev",
    "build": "bun --filter '*' build",
    "check": "bun --filter '*' check"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5"
  }
}
