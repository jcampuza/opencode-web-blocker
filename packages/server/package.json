{
  "name": "opencode-web-blocker-server",
  "version": "0.0.1",
  "type": "module",
  "main": "index.ts",
  "scripts": {
    "dev": "bun run index.ts ",
    "check": "tsc --noEmit",
    "build": "bun build --compile --minify index.ts --outfile dist/server.ts",
    "start": "./dist/server.ts"
  },
  "dependencies": {
    "opencode-web-blocker-shared": "workspace:*"
  }
}
